#include<stdio.h>

int summ(int n, int a[])
{
    int i;
    	int sum = 0;
	for(i=0;i<n;i++)
	{
	    
		sum +=  a[i];
		
	}
	return sum;
	
}

int main()
{
	int n;
	scanf("%d",&n);
	int a[n];
	int i;
	for(i=0;i<n;i++)
	{
		scanf("%d",&a[i]);
	}

    summ(n,a);

	printf("%d",summ(n,a));
	
}
